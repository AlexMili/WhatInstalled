from typing import Optional

# Missing:
# - apt list --installed
# - apt-get install
# - aptitude install
# - yum install
# - pacman install
# - dpkg -i
# - dpkg --list
# - dnf install
# - zypper in
# - pkgutil --pkgs


def all(excluded: Optional[list[str]] = []):
    all_pkgs = []

    return all_pkgs
